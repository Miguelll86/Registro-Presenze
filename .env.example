# Database PostgreSQL (obbligatorio per build e runtime)
# Crea un DB gratuito su https://neon.tech e incolla qui la connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Segreto per le sessioni (obbligatorio in produzione)
# Genera una stringa casuale: openssl rand -base64 32
JWT_SECRET="una-stringa-lunga-e-casuale"
